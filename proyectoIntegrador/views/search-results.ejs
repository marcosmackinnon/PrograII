<!DOCTYPE html>
<html lang="es">
<!-- Include Head -->

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<link width="" rel="shortcut icon" href="./images/logoheader.jpg" />

	<title> Mercedez Benz Store </title>
	<!-- Font Awesome Stylesheet -->
	<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.11.2/css/all.min.css">
	<!-- Bootstrap Grid Stylesheet -->
	<link rel="stylesheet" href="/stylesheets/bootstrap-grid.min.css">
	<!-- Own Stylesheet -->
	<link rel="stylesheet" href="/stylesheets/app.css">
</head>

<body>
	<!-- Header -->
	<%- include('partials/pHeaderNoLogueado') %>
	<!-- /Header -->

	<h2>Resultados para "<%= search %>"</h2>

	<!-- Novedades -->
	<div class="container products-wrapper">
		<div class="row">
			<div class="col-12">
				<h2 class="products-title"> Nuestros autos</h2>
			</div>
			<% if (data.length > 0) { %>
				<% for (let i = 0; i < data.length; i++) { %>
					<div class="col-12 col-sm-6 col-lg-3">
						<section class="product-box">
							<a href="/product/<%= data[i].id %>">
								<figure class="product-box_image">
									<img src="/images/autos/<%= data[i].imagen %>" alt="<%= data[i].nombre %>">
								</figure>
								<article class="product-box_data">
									<h2><%= data[i].nombre %></h2>
									<p><%= data[i].descripcion %></p>
									<p>Comentarios: <%= data[i].comentarios.length %></p>
									<p>
										Subido por: 
										<a href="/perfil/<%= data[i].usuario.id %>">
											<%= data[i].usuario.email %>
										</a>
									</p>
								</article>
							</a>
						</section>
					</div>
				<% } %>
			<% } else { %>
				<div class="col-12">
					<p>No hay resultados para su búsqueda.</p>
				</div>
			<% } %>
		</div>
	</div>
			  


	<!-- footer -->
	<%- include('partials/footer') %>
	<!-- /footer -->
</body>

</html>